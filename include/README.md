# geniex-proc include directory

This directory contains the **public** headers for the `geniex-proc` library.

For public usage, the headers under this directory should only includes public headers as well, such as:
- `xtensor-all.hpp`
- `tokenizers_cpp.h`
- `stb_image.h`, `stb_image_resize2.h`
- ...